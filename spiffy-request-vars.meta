;; -*- scheme -*-

((synopsis "Provides easy access to variables from HTTP requests")
 (author "Mario Domenech Goulart")
 (category web)
 (license "BSD")
 (depends (intarweb 0.3) uri-common spiffy)
 (test-depends server-test (awful "0.36") test http-client))
